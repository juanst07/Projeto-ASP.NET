@page "{id:int}"
@model TarefasApp.Pages.ExcluirModel

<div class="row justify-content-center">
    <div class="col-md-6">
        <div class="card shadow-sm border-0">
            <div class="card-header bg-white text-dark border-0">
                <h2 class="mb-0 fw-light">Confirmar Exclusão</h2>
            </div>
            <div class="card-body bg-light">
                <p>Tem certeza que deseja excluir a tarefa <b>@Model.Tarefa.Titulo</b>?</p>
                <form method="post" class="d-flex gap-2">
                    <button type="submit" class="btn btn-outline-danger">Sim, excluir</button>
                    <a asp-page="/Index" class="btn btn-outline-secondary">Cancelar</a>
                </form>
            </div>
        </div>
    </div>
</div>
